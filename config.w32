// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("xlog", "for xlog support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("xlog", "enable xlog support", "no");

if (PHP_XLOG != "no") {
	EXTENSION("xlog", "xlog.c");
}

